# Docker environment — copy to .env.docker and fill in
# NEVER commit real credentials

KITE_API_KEY=
KITE_API_SECRET=
KITE_ACCESS_TOKEN=
DATABASE_URL=postgresql+asyncpg://bot:changeme@db:5432/sbitb
POSTGRES_PASSWORD=changeme
